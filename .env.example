# ============================================================================
# MindRipper - Configuration Example
# ============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env file to version control!
# ============================================================================

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
NODE_ENV=production
PORT=3000
TZ=Europe/Paris
LOG_LEVEL=info

# ============================================================================
# SCRAPING CONFIGURATION
# ============================================================================
# Target URL to scrape (required)
TARGET_URL=https://example.com/article

# Cron expression for scheduled scraping (required)
# Examples:
#   - "0 9 * * *"    = Every day at 9:00 AM
#   - "0 */6 * * *"  = Every 6 hours
#   - "0 0 * * 1"    = Every Monday at midnight
SCRAPE_CRON=0 9 * * *

# ============================================================================
# LIBRETRANSLATE (Self-Hosted Translation - 100% Free & Unlimited)
# ============================================================================
# LibreTranslate runs as a Docker service alongside MindRipper
# - Completely FREE and UNLIMITED
# - No API key required
# - Automatically configured via docker-compose.yml
# - Only loads English and French models to save memory (~1GB RAM total)
#
# The docker-compose.yml automatically:
# - Starts LibreTranslate service
# - Configures URL to http://libretranslate:5000/translate
# - Connects both services via private network
#
# No configuration needed - it just works! 🚀

# ============================================================================
# NOTION API
# ============================================================================
# Create Notion integration:
# 1. Go to https://www.notion.so/my-integrations
# 2. Click "New integration"
# 3. Give it a name (e.g., "MindRipper")
# 4. Copy the "Internal Integration Token"
NOTION_API_KEY=secret_your-notion-api-key-here

# Get Database ID:
# 1. Open your Notion database in browser
# 2. Copy the URL, it looks like:
#    https://www.notion.so/{workspace}/{database_id}?v={view_id}
# 3. The database_id is the part between workspace and ?v=
# 4. Share the database with your integration
NOTION_DATABASE_ID=your-notion-database-id-here
